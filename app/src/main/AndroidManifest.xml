<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.cb006789styleomega">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminHome" />
        <activity android:name=".LoginSuccessActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Catergorys" />
        <activity android:name=".MainMenu" />
        <activity android:name=".AddProductsActivity" />
        <activity android:name=".AddToCartActivity" />
        <activity android:name=".ItemPage" />
        <activity android:name=".GetCardDetails" />
        <activity android:name=".InquiryActivity" />
        <activity android:name=".ViewInquiry" />
        <activity android:name=".ManageUsers" />
        <activity android:name=".ManageEmail" />
        <activity android:name=".About"></activity>
    </application>

</manifest>